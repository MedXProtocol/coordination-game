{
  "name": "coordination-game",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dapp": "cd dapp && npm run start",
    "compile": "$(npm bin)/truffle compile && apply-registry build/contracts",
    "migrate": "$(npm bin)/truffle migrate && apply-registry build/contracts",
    "staging-build": "./scripts/build.sh",
    "test": "npm run compile && $(npm bin)/truffle test",
    "start": "./scripts/ganache.sh",
    "lambda": "cd lambda && ./lambda-serve.sh"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ganache-cli": "^6.1.8",
    "zos": "^2.0.1",
    "truffle-hdwallet-provider": "^0.0.6"
  },
  "dependencies": {
    "abi-decoder": "^1.2.0",
    "debug": "^4.1.0",
    "ethereumjs-abi": "^0.6.5",
    "openzeppelin-eth": "^2.0.2",
    "zos-lib": "^2.0.1"
  }
}
